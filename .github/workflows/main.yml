name: CCcam Elite Hunter

on:
  schedule:
    - cron: '0 */4 * * *' # السكربت كيخدم لراسو كل 4 سوايع
  workflow_dispatch:      # كيخليك تخدمو بيديك (Run workflow) من GitHub
  push:
    branches:
      - main              # كيخدم ملي كتبدل شي حاجة في الكود

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests

    - name: Clean Old Files
      run: |
        rm -f VERIFIED_CANNON.cfg
        echo "Old file deleted successfully."

    - name: Run Scraper
      run: python scraper.py

    - name: Commit and Push
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add VERIFIED_CANNON.cfg
        # هاد السطر كيمنع السكربت يحبس إلا مالقاش تغييرات
        git commit -m "🚀 Update: New Elite Servers $(date +'%Y-%m-%d %H:%M')" || exit 0
        git push
